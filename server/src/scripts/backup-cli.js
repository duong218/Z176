/**
 * CLI backup — dùng chung logic với POST /admin/backup (sẽ triển khai sau).
 * DEMO-ONLY: Google Drive — xem SECURITY_BASELINE.md mục 4.
 */
console.error('[backup] Not implemented yet — schema/API review pending.');
process.exit(1);
